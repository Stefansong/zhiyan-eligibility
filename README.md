# 智研入排 · 智能患者入排系统 v2

临床试验患者入排资格 AI 辅助审核系统——可本地部署的完整产品。
病历照片**不上传**（OCR 本地完成），仅识别后的文字发送至 DeepSeek 进行结构化解析与逐条判定。

## 功能（完整审核流程）

1. **受试者队列** — 多项目仪表盘：统计卡、入排符合度色条、状态筛选、中心切换/新增
2. **病史上传 · 智能解析** — 上传病历照片 / PDF / Excel → 本地 OCR / 数据表导入 → AI 医学实体识别为结构化字段（每个字段带置信度和原文出处，可点击核验原件）
3. **方案 · 入排标准** — 上传研究方案 PDF / Word → AI 锚定提取入选/排除标准 → 逐条人工核对（可修改/删除）→ 锚定确认
4. **入排比对审核（核心）** — AI 逐条比对受试者证据与方案标准：判定（符合/存疑/不符合）+ 推理依据 + 病历原文引用 + 置信度；人工可逐条改判并自动保存；结论样式三选（色块行 / 卡片 / 双栏）
5. **审核意见报告** — 标准化意见书（最终裁定 = AI 判定 + 人工改判）：审核结论 / 需处理项与建议 / 全量明细 / 签字栏，浏览器打印导出 PDF

另含：多项目管理（新建项目从上传方案开始）、原文出处抽屉（真实原件：照片/PDF/Excel 直接预览 + 判定引用片段对照）、左下角个人资料（联动报告签字栏）。

## 安装与启动

### macOS / Linux

```bash
./start.sh
```

首次运行会自动创建虚拟环境、安装依赖、生成 `.env`——把 DeepSeek API Key 填进去（`DEEPSEEK_API_KEY=sk-xxxx`）后再次运行，浏览器自动打开 http://127.0.0.1:8780

### Windows

双击 `start.bat`，按提示在记事本中填入 API Key，再次双击启动。

### 获取 DeepSeek API Key

[platform.deepseek.com](https://platform.deepseek.com) 注册 → 充值少量金额 → 「API Keys」创建密钥。
首次打开为引导页，从「新建项目（上传研究方案）」开始使用——该步骤及后续 AI 功能需要已配置 Key。

## 使用流程

1. **新建项目**：侧边栏项目切换器 → 「新建项目（上传研究方案）」→ 上传方案 PDF/Word（或粘贴入排标准文本）→ AI 提取后在「方案 · 入排标准」页**逐条核对修改** → 「确认无误，锚定标准」
2. **受试者建档**：队列页「新增受试者」（编号请用筛选号，勿用真实姓名）→ 「病史上传 · 解析」页上传病历照片/PDF/Excel → 本地识别完成后点「AI 结构化解析」→ 核对字段（点「原文」可对照原件）
3. **智能审核**：「入排比对审核」页 → 「开始智能审核」→ 逐条查看判定/依据/原文出处 → 存疑项人工裁定（自动保存）
4. **出报告**：「审核意见报告」页 → 核对意见书 → 「导出 PDF 意见书」

## 架构

```
version2/
├── src/              # React 前端（Vite，设计系统见 src/tokens.css）
├── dist/             # 前端构建产物（后端直接服务，无需 Node 环境）
├── backend/
│   ├── main.py       # FastAPI：REST API + 静态服务 + 原件服务
│   └── core/         # ocr（RapidOCR/PyMuPDF/openpyxl 本地识别）
│                     # protocol_parser / patient_parser / matcher（DeepSeek 提示词）
│                     # store（JSON 文件存储）
├── data/             # 运行数据（项目/患者/上传原件），删除即清空
├── start.sh / start.bat
└── .env              # DEEPSEEK_API_KEY
```

前端开发模式：`npm install && npm run dev`（代理到 127.0.0.1:8780）；改完跑 `npm run build` 更新 dist/。

## 隐私与合规

- 病历**图片不出本机**：OCR 使用本地引擎（RapidOCR），PDF 文本层本地抽取，Excel 本地读取
- 识别后的**文字**发送至 DeepSeek 服务器做结构化与判定，正式使用前请确认公司数据合规要求
- 建议上传前遮挡/裁剪姓名、身份证号等直接身份信息；系统已要求 AI 输出以「患者」代替姓名
- 全部数据保存在本目录 `data/` 下，删除该目录即清空所有记录

> ⚠️ **免责声明**：本系统输出为 AI 辅助意见，仅供初筛参考。OCR 与大模型均可能出错，每条判定都附原文出处——请对照原件核验关键数值。最终入组决定必须由研究医生依据原始资料独立确认。

## 常见问题

| 问题 | 解决 |
|------|------|
| 顶部黄条提示未配置 API Key | 检查 `.env` 中 `DEEPSEEK_API_KEY=` 是否已填，填好后重启（重新运行 start 脚本） |
| OCR 识别乱码/漏字 | 照片尽量拍正、光线充足；解析后逐字段核对原文 |
| 方案太长提取不全 | 新建项目时用「粘贴文本」，只粘贴入排标准章节 |
| 安装依赖很慢 | 启动脚本默认使用清华镜像源，失败时自动回退官方源 |
| 想清空全部数据 | 删除 `data/` 目录后重启，回到首次使用引导页 |
